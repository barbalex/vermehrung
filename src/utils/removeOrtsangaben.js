const removeOrtsangaben = () => {
  // need to remove some data for gaertner in case they had synced them earlier
  // TODO: remove: lokalname, geom_point, lv95_x, lv95_y, wgs84_lat, wgs84_long
}

export default removeOrtsangaben
